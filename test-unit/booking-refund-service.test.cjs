'use strict';const{test}=require('node:test');const assert=require('node:assert/strict');const service=require('../functions-mobile/booking-refund-service.cjs');
const clone=value=>value==null?value:structuredClone(value);class Snapshot{constructor(value){this.value=clone(value)}val(){return clone(this.value)}}const parts=path=>String(path||'').split('/').filter(Boolean);function read(root,path){let node=root;for(const part of parts(path)){if(node==null)return null;node=node[part]}return node==null?null:node}function write(root,path,value){const keys=parts(path);if(!keys.length){for(const key of Object.keys(root))delete root[key];Object.assign(root,clone(value)||{});return}let node=root;for(const key of keys.slice(0,-1))node=node[key]||(node[key]={});const last=keys.at(-1);if(value===null)delete node[last];else node[last]=clone(value)}class Ref{constructor(db,path){this.db=db;this.path=path}async get(){return new Snapshot(read(this.db.data,this.path))}async transaction(update){const current=clone(read(this.db.data,this.path)),next=update(current);if(next===undefined)return{committed:false,snapshot:new Snapshot(current)};write(this.db.data,this.path,next);return{committed:true,snapshot:new Snapshot(next)}}async update(updates){if(this.db.failUpdateOnce){this.db.failUpdateOnce=false;throw Error('simulated network loss')}for(const[path,value]of Object.entries(updates))write(this.db.data,[this.path,path].filter(Boolean).join('/'),value)}}class FakeDb{constructor(data){this.data=clone(data);this.failUpdateOnce=false}ref(path=''){return new Ref(this,path)}}
const now=Date.parse('2026-10-01T12:00:00+07:00'),actorUid='coach_12345678',athleteId='athlete_12345678',proof={path:`private/refund-slip/${actorUid}/row/refund.jpg`,contentType:'image/jpeg',size:1024,name:'refund.jpg'};
function booking(changed={}){return{coachId:actorUid,athleteId,date:'2026-10-10',start:10,end:11,status:'confirmed',paymentStatus:'payment_verified',priceSatang:100000,travelFeeSatang:10000,platformFeeSatang:5000,...changed}}function seed(changed={}){return{users:{[actorUid]:{role:'coach',status:'active',subscription:{trialEndsAt:now+86400000}},[athleteId]:{role:'athlete',refundAccount:{bank:'ธนาคารทดสอบ',accountName:'Athlete Test',accountNumber:'0012345678'}}},bookings:{BOOKING_123:booking(changed)},coachSlotLocks:{[actorUid]:{'2026-10-10':{booking_BOOKING_123:{bookingId:'BOOKING_123',date:'2026-10-10',start:10,end:11,status:'active'}}}}}}const request=(action='coach_cancel',requestId='request_12345678',input={})=>({actorUid,input:{bookingId:'BOOKING_123',requestId,action,...input},now});
test('coach cancellation creates a server-derived refund request and releases the slot',async()=>{const db=new FakeDb(seed()),result=await service.execute(db,request());assert.equal(result.status,'cancelled_by_coach');assert.equal(result.refundStatus,'requested');assert.equal(read(db.data,'refunds/RF-BOOKING_123/amountSatang'),110000);assert.equal(read(db.data,`coachSlotLocks/${actorUid}/2026-10-10/booking_BOOKING_123`),null)});
test('coach cancellation retry repairs a missing refund record',async()=>{const db=new FakeDb(seed());db.failUpdateOnce=true;await assert.rejects(()=>service.execute(db,request()),/simulated network loss/);assert.equal(read(db.data,'bookings/BOOKING_123/status'),'cancelled_by_coach');const result=await service.execute(db,request());assert.equal(result.replay,true);assert.equal(read(db.data,'refunds/RF-BOOKING_123/status'),'requested')});
test('cash completion stores only private proof metadata and preserves read notification on replay',async()=>{const db=new FakeDb(seed({status:'refund_pending_coach_decision',refundStatus:'requested',refundRequestedAt:now}));write(db.data,'refunds/RF-BOOKING_123',{bookingId:'BOOKING_123',coachId:actorUid,athleteId,amountSatang:110000,status:'requested'});const req=request('coach_complete_cash_refund','request_87654321',{refundProof:proof}),first=await service.execute(db,req);assert.equal(first.status,'refunded');assert.equal(read(db.data,'refunds/RF-BOOKING_123/refundProofStorage/path'),proof.path);assert.equal(read(db.data,'bookings/BOOKING_123/refundProofDataUrl'),null);const nid=`booking_${actorUid}_request_87654321`;write(db.data,`notifications/${athleteId}/${nid}/read`,true);const again=await service.execute(db,req);assert.equal(again.replay,true);assert.equal(read(db.data,`notifications/${athleteId}/${nid}/read`),true)});
test('coin selection remains pending admin credit',async()=>{const db=new FakeDb(seed({status:'refund_pending_coach_decision',refundStatus:'requested',refundRequestedAt:now}));write(db.data,'refunds/RF-BOOKING_123',{bookingId:'BOOKING_123',coachId:actorUid,athleteId,amountSatang:110000,status:'requested'});const result=await service.execute(db,request('coach_select_coin_refund'));assert.equal(result.status,'refund_pending_admin_coin_credit');assert.equal(read(db.data,'refunds/RF-BOOKING_123/status'),'pending_admin_coin_credit')});
