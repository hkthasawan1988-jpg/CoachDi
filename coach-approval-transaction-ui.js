function c84AttentionRows(){return c80QueueReady()?c80Rows().filter(x=>x.needsApproval||['incomplete','reviewing'].includes(x.status)):[]}
c80PendingCount=function(){return c84AttentionRows().length};
c80InjectDashboard=function(){if(state.role!=='admin'||state.c47AdminPage!=='overview')return;const rows=c84AttentionRows(),pending=rows.filter(x=>x.needsApproval).length,issues=rows.length-pending,host=document.querySelector('#coachContent .c47Admin')||document.getElementById('coachContent');if(!host)return;let box=document.getElementById('c80DashAlert');if(!rows.length){box?.remove();return}if(!box){box=document.createElement('section');box.id='c80DashAlert';box.className='c80DashAlert';const first=host.querySelector('header');if(first)first.insertAdjacentElement('afterend',box);else host.prepend(box)}box.innerHTML=`<div><b>มี ${pending} คำขอ Coach พร้อมอนุมัติ${issues?` • ${issues} รายการต้องตรวจสอบข้อมูล`:''}</b><span>รายการใหม่และรายการสมัครไม่ครบอัปเดตอัตโนมัติ</span></div><button class="c80Button primary" onclick="s41ShowAdmin('coachApprovals')">เปิดคิวอนุมัติ</button>`};
async function c84FinalizeUser(uid,token,mutate){const result=await db.ref(`users/${uid}`).transaction(value=>{if(!value||c80Norm(value.status)!=='reviewing'||value.approvalClaimToken!==token)return;const next=mutate({...value});next.lastReviewToken=token;delete next.approvalClaimToken;delete next.approvalClaimedBy;delete next.approvalClaimedAt;delete next.approvalOriginalStatus;return next},undefined,false);if(!result.committed)throw Error('คำขอนี้ถูก Admin คนอื่นดำเนินการแล้ว กรุณารีเฟรช');return result.snapshot.val()||{}}
c81ReleaseClaim=async function(uid,token){const released=await db.ref(`users/${uid}`).transaction(value=>{if(!value||c80Norm(value.status)!=='reviewing'||value.approvalClaimToken!==token)return;const next={...value,status:value.approvalOriginalStatus||'pending_approval',lastReviewToken:token};delete next.approvalClaimToken;delete next.approvalClaimedBy;delete next.approvalClaimedAt;delete next.approvalOriginalStatus;return next},undefined,false);return released.committed};
c81ClaimCoach=async function(uid){const [us,ps]=await Promise.all([db.ref(`users/${uid}`).once('value'),db.ref(`coachProfiles/${uid}`).once('value')]);if(!us.exists()||!ps.exists())throw Error('ข้อมูลคำขอสมัครไม่ครบ จึงไม่สามารถอนุมัติได้');const initialUser=us.val()||{},initialProfile=ps.val()||{},decision=c82Decision(initialUser,initialProfile);if(initialUser.role!=='coach')throw Error('บัญชีนี้ไม่ใช่ Coach จึงไม่สามารถอนุมัติได้');if(!decision)throw Error('คำขอนี้ถูกดำเนินการหรือเปลี่ยนสถานะแล้ว กรุณารีเฟรช');const token=`${state.user.uid}:${c83Now()}:${Math.random().toString(36).slice(2)}`,claim=await db.ref(`users/${uid}`).transaction(value=>{const currentDecision=c82Decision(value||{},initialProfile);if(!value||value.role!=='coach'||!currentDecision)return;const currentOriginal=c80Norm(value.status)==='reviewing'?(value.approvalOriginalStatus||'pending_approval'):(value.status||'pending_approval');return{...value,status:'reviewing',approvalOriginalStatus:currentOriginal,approvalClaimToken:token,approvalClaimedBy:state.user.uid,approvalClaimedAt:c83Now()}},undefined,false);if(!claim.committed)throw Error('คำขอนี้กำลังถูก Admin คนอื่นดำเนินการ กรุณารีเฟรช');const latestProfile=(await db.ref(`coachProfiles/${uid}`).once('value')).val()||{},claimedUser=claim.snapshot.val()||{},storedOriginal=claimedUser.approvalOriginalStatus||'pending_approval',checkUser={...claimedUser,status:storedOriginal};if(!c82Decision(checkUser,latestProfile)){await c81ReleaseClaim(uid,token);throw Error('สถานะคำขอเปลี่ยนแล้ว กรุณารีเฟรช')}return{token,originalStatus:storedOriginal,user:claimedUser,profile:latestProfile,decision:c82Decision(checkUser,latestProfile)}};
c80ApproveCoach=async function(uid,button){if(state.role!=='admin'||!state.user?.uid)return alert('เฉพาะ Admin เท่านั้นที่อนุมัติ Coach ได้');if(button?.disabled)return;const original=button?.textContent||'อนุมัติ';let claim=null,finalized=false;try{if(button){button.disabled=true;button.textContent='กำลังเปิดใช้งาน...'}claim=await c81ClaimCoach(uid);const user=claim.user,profile=claim.profile,now=c83Now(),name=c82Text(profile.displayName,c82Text(profile.nameEn,c82Text(user.displayName,c82Text(user.email,'Coach').split('@')[0])));await c84FinalizeUser(uid,claim.token,current=>{const next={...current,status:'active',displayName:name,approvedBy:state.user.uid,approvedAt:now};delete next.rejectionReason;if(!current.subscription?.trialEndsAt&&!current.subscription?.currentPeriodEndsAt)next.subscription={status:'trial',priceSatang:19900,trialStartedAt:now,trialEndsAt:now+60*24*60*60*1000};return next});finalized=true;await db.ref(`coachProfiles/${uid}`).update({displayName:name,nameEn:c82Text(profile.nameEn,name),sport:c82Text(profile.sport,'tennis',40),status:'active',rejectionReason:null,reviewedBy:state.user.uid,reviewedAt:firebase.database.ServerValue.TIMESTAMP,updatedAt:firebase.database.ServerValue.TIMESTAMP});const [vu,vp]=await Promise.all([db.ref(`users/${uid}/status`).once('value'),db.ref(`coachProfiles/${uid}/status`).once('value')]);if(vu.val()!=='active'||vp.val()!=='active')throw Error('บันทึกสถานะได้บางส่วน กรุณากดซ่อมสถานะจากคิวอนุมัติ');try{await c80NotifyCoach(uid,'coach_account_approved','Admin อนุมัติบัญชี Coach แล้ว คุณสามารถเข้าสู่ระบบและเริ่มทดลองใช้ฟรี 60 วันได้')}catch(error){console.warn('Coach approval notification unavailable',error)}try{await audit('admin_coach_activated',uid,{source:'coach_approval_inbox',previousUserStatus:claim.originalStatus,previousProfileStatus:profile.status||'',decision:claim.decision})}catch(_){}if(typeof s42Toast==='function')s42Toast(claim.decision==='repair'?'ซ่อมสถานะและเปิดใช้งาน Coach สำเร็จ':'อนุมัติและเปิดใช้งาน Coach สำเร็จ');else alert('อนุมัติและเปิดใช้งาน Coach สำเร็จ');if(document.getElementById('c54Grid'))await c54Load();if(document.getElementById('s41CustomerTableHost'))await s41LoadCustomers()}catch(error){if(claim&&!finalized)try{await c81ReleaseClaim(uid,claim.token)}catch(_){}alert('อนุมัติ Coach ไม่สำเร็จ: '+(error.message||error));if(button){button.disabled=false;button.textContent=original}}};
c80RejectCoach=async function(uid,button){if(state.role!=='admin'||!state.user?.uid)return alert('เฉพาะ Admin เท่านั้นที่ตรวจสอบ Coach ได้');const reason=prompt('เหตุผลที่ไม่อนุมัติบัญชี Coach');if(reason===null||!confirm('ยืนยันไม่อนุมัติบัญชี Coach นี้?'))return;const original=button?.textContent||'ปฏิเสธ';let claim=null,finalized=false;try{if(button){button.disabled=true;button.textContent='กำลังบันทึก...'}claim=await c81ClaimCoach(uid);if(claim.decision==='repair'){await c81ReleaseClaim(uid,claim.token);claim=null;throw Error('รายการนี้ผ่านการอนุมัติบางส่วนแล้ว จึงซ่อมสถานะได้อย่างเดียว')}const note=reason.trim()||'ข้อมูลยังไม่ผ่านการตรวจสอบ',now=c83Now();await c84FinalizeUser(uid,claim.token,current=>({...current,status:'rejected',rejectionReason:note,reviewedBy:state.user.uid,reviewedAt:now}));finalized=true;await db.ref(`coachProfiles/${uid}`).update({status:'rejected',rejectionReason:note,reviewedBy:state.user.uid,reviewedAt:firebase.database.ServerValue.TIMESTAMP,updatedAt:firebase.database.ServerValue.TIMESTAMP});const [vu,vp]=await Promise.all([db.ref(`users/${uid}/status`).once('value'),db.ref(`coachProfiles/${uid}/status`).once('value')]);if(vu.val()!=='rejected'||vp.val()!=='rejected')throw Error('บันทึกสถานะได้บางส่วน กรุณารีเฟรชเพื่อตรวจสอบ');try{await c80NotifyCoach(uid,'coach_account_rejected','คำขอสมัคร Coach ยังไม่ผ่านการอนุมัติ กรุณาติดต่อ Admin เพื่อตรวจสอบข้อมูล')}catch(error){console.warn('Coach rejection notification unavailable',error)}try{await audit('admin_coach_rejected',uid,{reason:note,source:'coach_approval_inbox'})}catch(_){}if(typeof s42Toast==='function')s42Toast('บันทึกผลการตรวจสอบแล้ว')}catch(error){if(claim&&!finalized)try{await c81ReleaseClaim(uid,claim.token)}catch(_){}alert('บันทึกผลไม่สำเร็จ: '+(error.message||error));if(button){button.disabled=false;button.textContent=original}}};
c77ApproveCoach=c80ApproveCoach;c78ApproveCustomerCoach=c80ApproveCoach;
