[
  {
    "before": "<script src=\"https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js\"></script>",
    "after": "<script src=\"auth-session.js\"></script>\n<script src=\"https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js\"></script>"
  },
  {
    "before": "const firebasePortalScope=[\"athlete\",\"coach\",\"admin\"].includes(requestedPortal)?requestedPortal:\"athlete\";",
    "after": "const sessionEntry=CoachDiSession.entry(location.search);\nconst firebasePortalScope=sessionEntry.scope;"
  },
  {
    "before": "let selectedLoginPortal=[\"athlete\",\"coach\",\"admin\"].includes(requestedPortal)?requestedPortal:\"athlete\";",
    "after": "let selectedLoginPortal=sessionEntry.role;\nlet sessionLoginPending=false,sessionLoginIntent=null,sessionBootstrapRevision=0,sessionBootstrapBusy=false;\nCoachDiSession.status(\"กำลังตรวจสอบการเข้าสู่ระบบเดิม...\");"
  },
  {
    "before": "if(remember)remember.checked=Boolean(saved)||remember.checked;",
    "after": "if(remember)remember.checked=CoachDiSession.keep(firebasePortalScope);"
  },
  {
    "before": "async function login(){try{const email=loginId.value.trim(),password=loginPass.value,remember=document.getElementById(\"rememberLogin\")?.checked!==false;if(!email||!password)throw new Error(\"กรุณากรอก Email และ Password\");authMessage.textContent=\"กำลังเข้าสู่ระบบ...\";await auth.setPersistence(remember?firebase.auth.Auth.Persistence.LOCAL:firebase.auth.Auth.Persistence.SESSION);try{remember?localStorage.setItem(C99_LOGIN_EMAIL_KEY,email):localStorage.removeItem(C99_LOGIN_EMAIL_KEY)}catch(_){}await auth.signInWithEmailAndPassword(email,password)}catch(e){authMessage.textContent=thaiAuthError(e)}}",
    "after": "async function login(){\n if(sessionLoginPending)return;\n try{\n  const email=loginId.value.trim(),password=loginPass.value,remember=document.getElementById(\"rememberLogin\")?.checked!==false;\n  if(!email||!password)throw new Error(\"กรุณากรอก Email และ Password\");\n  sessionLoginPending=true;sessionLoginIntent=selectedLoginPortal;loginBtn.disabled=true;\n  authMessage.textContent=\"กำลังเข้าสู่ระบบ...\";\n  await auth.setPersistence(remember?firebase.auth.Auth.Persistence.LOCAL:firebase.auth.Auth.Persistence.SESSION);\n  CoachDiSession.preference(firebasePortalScope,remember);\n  try{remember?localStorage.setItem(C99_LOGIN_EMAIL_KEY,email):localStorage.removeItem(C99_LOGIN_EMAIL_KEY)}catch(_){}\n  await auth.signInWithEmailAndPassword(email,password);\n }catch(e){authMessage.textContent=thaiAuthError(e)}\n finally{sessionLoginPending=false;sessionLoginIntent=null;loginBtn.disabled=false}\n}"
  },
  {
    "before": "async function logout(){await auth.signOut()}",
    "after": "async function logout(){await auth.signOut();CoachDiSession.forget(firebasePortalScope)}"
  },
  {
    "before": "auth.onAuthStateChanged(async user=>{try{if(!user){state.user=null;state.role=null;portal.classList.add(\"hidden\");loginView.classList.remove(\"hidden\");logoutBtn.classList.add(\"hidden\");selectLoginPortal(selectedLoginPortal);return}state.user=user;authMessage.textContent=\"ตรวจสอบสิทธิ์ผู้ใช้งาน...\";let ref=db.ref(`users/${user.uid}`),snap=await ref.once(\"value\"),profile=snap.val();if(selectedLoginPortal===\"coach\")profile=await ensureCoachApprovalRequest(user,ref,profile);if(!profile?.role&&selectedLoginPortal===\"athlete\"){await ref.set({role:\"athlete\",status:\"active\",email:user.email||\"\",createdAt:firebase.database.ServerValue.TIMESTAMP});snap=await ref.once(\"value\");profile=snap.val()}if(!profile?.role){await auth.signOut();authMessage.textContent=selectedLoginPortal===\"coach\"?\"ระบบยังสร้างบัญชี Coach ไม่สำเร็จ กรุณาลองเข้าสู่ระบบอีกครั้ง\":\"บัญชีนี้ยังไม่ได้รับสิทธิ์ Admin — ให้กำหนด role = admin ใน Firebase\";return}state.role=String(profile.role).trim().toLowerCase();if(![\"athlete\",\"coach\",\"admin\"].includes(state.role)){await auth.signOut();authMessage.textContent=\"Role ของบัญชีนี้ไม่ถูกต้อง กรุณาติดต่อ Admin\";return}if(state.role!==selectedLoginPortal){let actual=state.role;await auth.signOut();authMessage.textContent=`บัญชีนี้เป็น ${actual} ไม่สามารถเข้า ${selectedLoginPortal} Portal ได้`;return}if(state.role===\"coach\"&&String(profile.status||\"active\").toLowerCase()!==\"active\"){const coachStatus=String(profile.status||\"pending_approval\").toLowerCase();await auth.signOut();authMessage.textContent=coachStatus===\"rejected\"?\"บัญชี Coach ถูกปฏิเสธ กรุณาติดต่อ Admin\":coachStatus===\"suspended\"?\"บัญชี Coach ถูกระงับการใช้งาน กรุณาติดต่อ Admin\":\"บัญชี Coach ยังเปิดใช้งานไม่สมบูรณ์ กรุณาลองอีกครั้ง\";return}authMessage.textContent=\"กำลังโหลดข้อมูล...\";await loadFirebaseData();enterPortal();authMessage.textContent=\"เข้าสู่ระบบสำเร็จ\"}catch(e){console.error(\"Coach Di login bootstrap error\",e);state.authBootstrapError=e;loginView.classList.remove(\"hidden\");portal.classList.add(\"hidden\");logoutBtn.classList.remove(\"hidden\");authMessage.innerHTML=(selectedLoginPortal===\"coach\"?'เข้าสู่ระบบ Coach ไม่สำเร็จ: ':'')+'เชื่อมต่อข้อมูลไม่สำเร็จชั่วคราว แต่ระบบยังคงการเข้าสู่ระบบไว้ <button type=\"button\" class=\"pill\" style=\"margin-left:6px\" onclick=\"location.reload()\">ลองโหลดอีกครั้ง</button>'}});",
    "after": "async function restoreCoachDiSession(user,loginIntent=null){\n const revision=++sessionBootstrapRevision;\n const current=()=>revision===sessionBootstrapRevision&&auth.currentUser?.uid===user?.uid;\n sessionBootstrapBusy=Boolean(user);state.authBootstrapError=null;\n if(!user){\n  state.user=null;state.role=null;CoachDiSession.finish();portal.classList.add(\"hidden\");\n  loginView.classList.remove(\"hidden\");logoutBtn.classList.add(\"hidden\");selectLoginPortal(selectedLoginPortal);return;\n }\n state.user=user;state.role=null;\n CoachDiSession.status(\"กำลังโหลดบัญชีและข้อมูลของคุณ...\");logoutBtn.classList.remove(\"hidden\");\n try{\n  // Firebase owns the credentials. Only the user's persistence preference and route are stored by this app.\n  const remember=CoachDiSession.keep(firebasePortalScope);\n  await auth.setPersistence(remember?firebase.auth.Auth.Persistence.LOCAL:firebase.auth.Auth.Persistence.SESSION);\n  if(!current())return;\n  const ref=db.ref(`users/${user.uid}`);let profile=(await ref.once(\"value\")).val();\n  if(!current())return;\n  const restoredRole=String(profile?.role||\"\").trim().toLowerCase();\n  if(!loginIntent&&[\"athlete\",\"coach\",\"admin\"].includes(restoredRole))selectLoginPortal(restoredRole);\n  if(selectedLoginPortal===\"coach\")profile=await ensureCoachApprovalRequest(user,ref,profile);\n  if(!current())return;\n  if(!profile?.role&&selectedLoginPortal===\"athlete\"){\n   await ref.set({role:\"athlete\",status:\"active\",email:user.email||\"\",createdAt:firebase.database.ServerValue.TIMESTAMP});\n   if(!current())return;profile=(await ref.once(\"value\")).val();\n  }\n  if(!current())return;\n  if(!profile?.role){await auth.signOut();authMessage.textContent=selectedLoginPortal===\"coach\"?\"ระบบยังสร้างบัญชี Coach ไม่สำเร็จ กรุณาลองเข้าสู่ระบบอีกครั้ง\":\"บัญชีนี้ยังไม่ได้รับสิทธิ์ Admin — ให้กำหนด role = admin ใน Firebase\";return}\n  const role=String(profile.role).trim().toLowerCase();\n  if(![\"athlete\",\"coach\",\"admin\"].includes(role)){await auth.signOut();authMessage.textContent=\"Role ของบัญชีนี้ไม่ถูกต้อง กรุณาติดต่อ Admin\";return}\n  // A restored session follows the verified profile; an explicit sign-in still validates the chosen portal.\n  if(loginIntent&&role!==loginIntent){await auth.signOut();authMessage.textContent=`บัญชีนี้เป็น ${role} ไม่สามารถเข้า ${loginIntent} Portal ได้`;return}\n  if(role===\"coach\"&&String(profile.status||\"active\").toLowerCase()!==\"active\"){\n   const coachStatus=String(profile.status||\"pending_approval\").toLowerCase();await auth.signOut();\n   authMessage.textContent=coachStatus===\"rejected\"?\"บัญชี Coach ถูกปฏิเสธ กรุณาติดต่อ Admin\":coachStatus===\"suspended\"?\"บัญชี Coach ถูกระงับการใช้งาน กรุณาติดต่อ Admin\":\"บัญชี Coach ยังเปิดใช้งานไม่สมบูรณ์ กรุณาลองอีกครั้ง\";return;\n  }\n  state.role=role;selectLoginPortal(role);CoachDiSession.remember(firebasePortalScope,role,remember);\n  await loadFirebaseData();if(!current())return;\n  CoachDiSession.finish();enterPortal();authMessage.textContent=\"เข้าสู่ระบบสำเร็จ\";\n }catch(e){\n  if(!current())return;\n  console.error(\"Coach Di login bootstrap error\",e);state.authBootstrapError=e;\n  CoachDiSession.status(\"เชื่อมต่อข้อมูลไม่ได้ชั่วคราว กรุณาตรวจสอบอินเทอร์เน็ต คุณไม่ต้องกรอกรหัสผ่านใหม่\",retryCoachDiSession);\n }finally{if(revision===sessionBootstrapRevision)sessionBootstrapBusy=false}\n}\nfunction retryCoachDiSession(){if(auth.currentUser&&!sessionBootstrapBusy)return restoreCoachDiSession(auth.currentUser)}\nauth.onAuthStateChanged(user=>restoreCoachDiSession(user,sessionLoginIntent));\nwindow.addEventListener('online',()=>{if(state.authBootstrapError)retryCoachDiSession()});\ndocument.addEventListener('visibilitychange',()=>{if(!document.hidden&&state.authBootstrapError)retryCoachDiSession()});"
  }
]
