state.c83ServerOffset=0;state.c83LeaseTimer=0;
function c83Now(){return Date.now()+Number(state.c83ServerOffset||0)}
c80Date=function(value){const time=Number(value);if(!Number.isFinite(time)||time<0||time>8640000000000000)return'-';const date=new Date(time);if(Number.isNaN(date.getTime()))return'-';try{return new Intl.DateTimeFormat('th-TH',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Bangkok'}).format(date)}catch(_){return'-'}};
c82EffectiveUserStatus=function(user={}){const status=c80Norm(user.status||'pending_approval');if(status!=='reviewing')return status;const claimedAt=Number(user.approvalClaimedAt||0),stale=Number.isFinite(claimedAt)&&claimedAt>0&&claimedAt<c83Now()-C82_REVIEW_LEASE_MS;return stale?c80Norm(user.approvalOriginalStatus||'pending_approval'):'reviewing'};
function c83ScheduleLeaseRefresh(){clearTimeout(state.c83LeaseTimer);if(state.role!=='admin')return;const expiries=Object.values(state.c80Users||{}).filter(user=>c80Norm(user?.status)==='reviewing').map(user=>Number(user.approvalClaimedAt||0)+C82_REVIEW_LEASE_MS-c83Now()).filter(delay=>Number.isFinite(delay)&&delay>0);if(expiries.length)state.c83LeaseTimer=setTimeout(()=>c80Render(),Math.max(50,Math.min(...expiries)+50))}
const c83RenderBase=c80Render;c80Render=function(){const result=c83RenderBase();c83ScheduleLeaseRefresh();return result};
const c83StopBase=c80Stop;c80Stop=function(){clearTimeout(state.c83LeaseTimer);return c83StopBase()};
const c83BindBase=c80Bind;c80Bind=function(){const result=c83BindBase();if(state.role==='admin')db.ref('.info/serverTimeOffset').once('value').then(snapshot=>{state.c83ServerOffset=Number(snapshot.val()||0);c80Render()}).catch(()=>{});return result};
c81ReleaseClaim=async function(uid,token,originalStatus){const released=await db.ref(`users/${uid}`).transaction(value=>{if(!value||c80Norm(value.status)!=='reviewing'||value.approvalClaimToken!==token)return;const next={...value,status:originalStatus||'pending_approval',lastReviewToken:token};delete next.approvalClaimToken;delete next.approvalClaimedBy;delete next.approvalClaimedAt;delete next.approvalOriginalStatus;return next},undefined,false);return released.committed};
c81ClaimCoach=async function(uid){const [us,ps]=await Promise.all([db.ref(`users/${uid}`).once('value'),db.ref(`coachProfiles/${uid}`).once('value')]);if(!us.exists()||!ps.exists())throw Error('ข้อมูลคำขอสมัครไม่ครบ จึงไม่สามารถอนุมัติได้');const user=us.val()||{},profile=ps.val()||{},decision=c82Decision(user,profile);if(user.role!=='coach')throw Error('บัญชีนี้ไม่ใช่ Coach จึงไม่สามารถอนุมัติได้');if(!decision)throw Error('คำขอนี้ถูกดำเนินการหรือเปลี่ยนสถานะแล้ว กรุณารีเฟรช');const token=`${state.user.uid}:${c83Now()}:${Math.random().toString(36).slice(2)}`,originalStatus=c80Norm(user.status)==='reviewing'?(user.approvalOriginalStatus||'pending_approval'):(user.status||'pending_approval'),claim=await db.ref(`users/${uid}`).transaction(value=>{const currentDecision=c82Decision(value||{},profile);if(!value||value.role!=='coach'||!currentDecision)return;const currentOriginal=c80Norm(value.status)==='reviewing'?(value.approvalOriginalStatus||'pending_approval'):(value.status||'pending_approval');return{...value,status:'reviewing',approvalOriginalStatus:currentOriginal,approvalClaimToken:token,approvalClaimedBy:state.user.uid,approvalClaimedAt:c83Now()}},undefined,false);if(!claim.committed)throw Error('คำขอนี้กำลังถูก Admin คนอื่นดำเนินการ กรุณารีเฟรช');const latestProfile=(await db.ref(`coachProfiles/${uid}`).once('value')).val()||{},claimedUser=claim.snapshot.val()||{},checkUser={...claimedUser,status:claimedUser.approvalOriginalStatus||originalStatus};if(!c82Decision(checkUser,latestProfile)){await c81ReleaseClaim(uid,token,originalStatus);throw Error('สถานะคำขอเปลี่ยนแล้ว กรุณารีเฟรช')}return{token,originalStatus,user:claimedUser,profile:latestProfile,decision:c82Decision(checkUser,latestProfile)}};
c80ApproveCoach=async function(uid,button){if(state.role!=='admin'||!state.user?.uid)return alert('เฉพาะ Admin เท่านั้นที่อนุมัติ Coach ได้');if(button?.disabled)return;const original=button?.textContent||'อนุมัติ';let claim=null,decided=false;try{if(button){button.disabled=true;button.textContent='กำลังเปิดใช้งาน...'}claim=await c81ClaimCoach(uid);const user=claim.user,profile=claim.profile,now=c83Now(),name=c82Text(profile.displayName,c82Text(profile.nameEn,c82Text(user.displayName,c82Text(user.email,'Coach').split('@')[0]))),updates={};updates[`users/${uid}/status`]='active';updates[`users/${uid}/displayName`]=name;updates[`users/${uid}/approvedBy`]=state.user.uid;updates[`users/${uid}/approvedAt`]=firebase.database.ServerValue.TIMESTAMP;updates[`users/${uid}/lastReviewToken`]=claim.token;updates[`users/${uid}/approvalClaimToken`]=null;updates[`users/${uid}/approvalClaimedBy`]=null;updates[`users/${uid}/approvalClaimedAt`]=null;updates[`users/${uid}/approvalOriginalStatus`]=null;updates[`coachProfiles/${uid}/displayName`]=name;updates[`coachProfiles/${uid}/nameEn`]=c82Text(profile.nameEn,name);updates[`coachProfiles/${uid}/sport`]=c82Text(profile.sport,'tennis',40);updates[`coachProfiles/${uid}/status`]='active';updates[`coachProfiles/${uid}/reviewedBy`]=state.user.uid;updates[`coachProfiles/${uid}/reviewedAt`]=firebase.database.ServerValue.TIMESTAMP;updates[`coachProfiles/${uid}/updatedAt`]=firebase.database.ServerValue.TIMESTAMP;if(!user.subscription?.trialEndsAt&&!user.subscription?.currentPeriodEndsAt)updates[`users/${uid}/subscription`]={status:'trial',priceSatang:19900,trialStartedAt:now,trialEndsAt:now+60*24*60*60*1000};await db.ref().update(updates);decided=true;const [vu,vp]=await Promise.all([db.ref(`users/${uid}/status`).once('value'),db.ref(`coachProfiles/${uid}/status`).once('value')]);if(vu.val()!=='active'||vp.val()!=='active')throw Error('ตรวจสอบสถานะหลังอนุมัติไม่สำเร็จ');try{await c80NotifyCoach(uid,'coach_account_approved','Admin อนุมัติบัญชี Coach แล้ว คุณสามารถเข้าสู่ระบบและเริ่มทดลองใช้ฟรี 60 วันได้')}catch(error){console.warn('Coach approval notification unavailable',error)}try{await audit('admin_coach_activated',uid,{source:'coach_approval_inbox',previousUserStatus:claim.originalStatus,previousProfileStatus:profile.status||'',decision:claim.decision})}catch(_){}if(typeof s42Toast==='function')s42Toast(claim.decision==='repair'?'ซ่อมสถานะและเปิดใช้งาน Coach สำเร็จ':'อนุมัติและเปิดใช้งาน Coach สำเร็จ');else alert('อนุมัติและเปิดใช้งาน Coach สำเร็จ');if(document.getElementById('c54Grid'))await c54Load();if(document.getElementById('s41CustomerTableHost'))await s41LoadCustomers()}catch(error){if(claim&&!decided)try{await c81ReleaseClaim(uid,claim.token,claim.originalStatus)}catch(_){}alert('อนุมัติ Coach ไม่สำเร็จ: '+(error.message||error));if(button){button.disabled=false;button.textContent=original}}};
c80RejectCoach=async function(uid,button){if(state.role!=='admin'||!state.user?.uid)return alert('เฉพาะ Admin เท่านั้นที่ตรวจสอบ Coach ได้');const reason=prompt('เหตุผลที่ไม่อนุมัติบัญชี Coach');if(reason===null||!confirm('ยืนยันไม่อนุมัติบัญชี Coach นี้?'))return;const original=button?.textContent||'ปฏิเสธ';let claim=null,decided=false;try{if(button){button.disabled=true;button.textContent='กำลังบันทึก...'}claim=await c81ClaimCoach(uid);if(claim.decision==='repair'){await c81ReleaseClaim(uid,claim.token,claim.originalStatus);claim=null;throw Error('รายการนี้ผ่านการอนุมัติบางส่วนแล้ว จึงซ่อมสถานะได้อย่างเดียว')}const text=reason.trim()||'ข้อมูลยังไม่ผ่านการตรวจสอบ',updates={};updates[`users/${uid}/status`]='rejected';updates[`users/${uid}/rejectionReason`]=text;updates[`users/${uid}/reviewedBy`]=state.user.uid;updates[`users/${uid}/reviewedAt`]=firebase.database.ServerValue.TIMESTAMP;updates[`users/${uid}/lastReviewToken`]=claim.token;updates[`users/${uid}/approvalClaimToken`]=null;updates[`users/${uid}/approvalClaimedBy`]=null;updates[`users/${uid}/approvalClaimedAt`]=null;updates[`users/${uid}/approvalOriginalStatus`]=null;updates[`coachProfiles/${uid}/status`]='rejected';updates[`coachProfiles/${uid}/rejectionReason`]=text;updates[`coachProfiles/${uid}/reviewedBy`]=state.user.uid;updates[`coachProfiles/${uid}/reviewedAt`]=firebase.database.ServerValue.TIMESTAMP;updates[`coachProfiles/${uid}/updatedAt`]=firebase.database.ServerValue.TIMESTAMP;await db.ref().update(updates);decided=true;const [vu,vp]=await Promise.all([db.ref(`users/${uid}/status`).once('value'),db.ref(`coachProfiles/${uid}/status`).once('value')]);if(vu.val()!=='rejected'||vp.val()!=='rejected')throw Error('ตรวจสอบสถานะหลังบันทึกไม่สำเร็จ');try{await c80NotifyCoach(uid,'coach_account_rejected','คำขอสมัคร Coach ยังไม่ผ่านการอนุมัติ กรุณาติดต่อ Admin เพื่อตรวจสอบข้อมูล')}catch(error){console.warn('Coach rejection notification unavailable',error)}try{await audit('admin_coach_rejected',uid,{reason:text,source:'coach_approval_inbox'})}catch(_){}if(typeof s42Toast==='function')s42Toast('บันทึกผลการตรวจสอบแล้ว')}catch(error){if(claim&&!decided)try{await c81ReleaseClaim(uid,claim.token,claim.originalStatus)}catch(_){}alert('บันทึกผลไม่สำเร็จ: '+(error.message||error));if(button){button.disabled=false;button.textContent=original}}};
c77ApproveCoach=c80ApproveCoach;c78ApproveCustomerCoach=c80ApproveCoach;
async function c83RemoveIfExists(ref){const snapshot=await ref.once('value');if(snapshot.exists())await ref.remove()}
c68RegisterCoach=async function(){const btn=document.getElementById('c68Submit'),msg=document.getElementById('c68Message'),name=document.getElementById('c68Name')?.value.trim()||'',sport=document.getElementById('c68Sport')?.value||'tennis',email=document.getElementById('c68Email')?.value.trim().toLowerCase()||'',pass=document.getElementById('c68Pass')?.value||'';if(!btn||btn.disabled)return;let regApp,regAuth,regDb,credential,uid='',failure=null,committed=false,cleanupFailed=false;try{if(name.length<2)throw Error('กรุณากรอกชื่อภาษาอังกฤษ');if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email))throw Error('รูปแบบ Email ไม่ถูกต้อง');if(pass.length<8)throw Error('Password ต้องมีอย่างน้อย 8 ตัวอักษร');if(pass!==(document.getElementById('c68Pass2')?.value||''))throw Error('Password ทั้งสองช่องไม่ตรงกัน');if(!document.getElementById('c68Accept')?.checked)throw Error('กรุณายอมรับข้อกำหนดก่อนสร้างบัญชี');btn.disabled=true;msg.textContent='กำลังสร้างบัญชีและส่งคำขอให้ Admin...';regApp=firebase.apps.find(x=>x.name==='coachRegistration')||firebase.initializeApp(firebaseConfig,'coachRegistration');regAuth=regApp.auth();regDb=regApp.database();credential=await regAuth.createUserWithEmailAndPassword(email,pass);uid=credential.user.uid;const stamp=firebase.database.ServerValue.TIMESTAMP;await regDb.ref(`users/${uid}`).set({role:'coach',status:'pending_approval',email,displayName:name,registrationComplete:false,createdAt:stamp});await regDb.ref(`coachProfiles/${uid}`).set({displayName:name,nameEn:name,sport,status:'pending_approval',registrationComplete:false,createdAt:stamp,updatedAt:stamp});const complete={};complete[`users/${uid}/registrationComplete`]=true;complete[`users/${uid}/registrationCompletedAt`]=stamp;complete[`coachProfiles/${uid}/registrationComplete`]=true;complete[`coachProfiles/${uid}/updatedAt`]=stamp;await regDb.ref().update(complete);committed=true}catch(error){failure=error;if(uid&&regDb&&!committed){try{await c83RemoveIfExists(regDb.ref(`coachProfiles/${uid}`));await c83RemoveIfExists(regDb.ref(`users/${uid}`))}catch(_){cleanupFailed=true}if(!cleanupFailed){try{await credential?.user?.delete()}catch(_){cleanupFailed=true}}}if(cleanupFailed)failure=Error('สร้างบัญชีไม่สมบูรณ์ กรุณาติดต่อ Admin โดยใช้อีเมลนี้เพื่อให้ตรวจสอบ')}finally{try{await regAuth?.signOut()}catch(_){}try{await regApp?.delete()}catch(_){}}if(failure){msg.textContent=thaiAuthError(failure);btn.disabled=false;return}loginId.value=email;authMessage.textContent='สร้างบัญชี Coach แล้ว • รอ Admin อนุมัติก่อนเข้าสู่ระบบ';const dialog=document.querySelector('#c68Modal .c68Dialog');if(dialog)dialog.innerHTML=`<div class="c80RegisterSuccess" role="status" aria-live="polite"><div class="c80RegisterSuccessIcon">✓</div><h2>ส่งคำขอสมัคร Coach แล้ว</h2><p>ระบบบันทึกบัญชีและส่งเข้าคิว Admin เรียบร้อย<br>คุณจะเข้าสู่ระบบได้หลัง Admin อนุมัติ</p><div class="c80RegisterStatus">สถานะ: รอ Admin อนุมัติ</div><button class="pill primary" style="width:100%;min-height:48px" onclick="c68CloseCoachRegistration()">กลับหน้าเข้าสู่ระบบ</button></div>`};
